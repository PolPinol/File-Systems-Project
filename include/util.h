#include <time.h>

#define TRUE 1
#define FALSE 0

/**
* Function that returns the current time in a string format
* @param t the time to be formatted
* @return the formatted time
*/
char *format_time(time_t t);
